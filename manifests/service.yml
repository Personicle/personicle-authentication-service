apiVersion: v1
kind: Service
metadata:
    name: "akspersonicledev-bccd"
    labels:
        app: "akspersonicledev-bccd"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akspersonicledev-bccd"