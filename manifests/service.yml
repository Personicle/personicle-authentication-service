apiVersion: v1
kind: Service
metadata:
    name: "personiclecluster-b0d8"
    labels:
        app: "personiclecluster-b0d8"
spec:
    type: LoadBalancer
    ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
    selector:
        app: "personiclecluster-b0d8"